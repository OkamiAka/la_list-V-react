class creatPop {
  constructor(nom, num, uti) {
    this.nom = nom;
    this.num = num;
    this.uti = uti;
  }
}

const pop = [new creatPop("pop 1", 1, 0), new creatPop("pop 2", 2, 2)];

export default pop;
